// Create a Flyable interface
// it should have land, fly and takeOff methods (TakeOff in C#)

interface FlyableInterface {
    land(): void;
    fly(): void;
    // takerOff(): void;
}

export { FlyableInterface };
